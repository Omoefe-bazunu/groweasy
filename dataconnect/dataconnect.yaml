specVersion: "v1"
serviceId: "groweasy"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "groweasy-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
